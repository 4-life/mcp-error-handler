services:
  mcp-error-handler:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./config/apps:/app/config/apps:ro
      - ./github-app-private-key.pem:/app/github-app-private-key.pem:ro
      - data:/app/data

volumes:
  data: